import { describe, it } from "vitest";

describe("InventoryDiffService", () => {
  it.skip("compares required materials against a player inventory snapshot", () => {
    // Pending until inventory snapshot fixtures exist.
  });
});
