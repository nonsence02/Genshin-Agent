import { describe, it } from "vitest";

describe("ResinOptimizerService", () => {
  it.skip("prioritizes deterministic resin tasks from planner requirements", () => {
    // Pending until requirement, source, and calendar fixtures exist.
  });
});
