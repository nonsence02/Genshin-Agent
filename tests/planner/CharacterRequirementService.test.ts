import { describe, it } from "vitest";

describe("CharacterRequirementService", () => {
  it.skip("calculates deterministic character ascension and talent requirements", () => {
    // Pending until normalized character cost fixtures exist.
  });
});
